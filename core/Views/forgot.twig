{ extends 'base.twig' }

{ block title}Forgot Password{ endblock }

{ block content }
<div class='container'>
	<div class="col-md-6 col-lg-6">
		<div class='panel panel-default'>
		<div class='panel-heading'><strong>Forgot Password</strong></div>
		<div class='panel-body'>
			<form role='form' method='POST' action='/forgot' id='forgotf'>
				<div class='form-group'>
					<label for="email">Enter Your Email Address</label>
					<input type="email" name="email" id="email" class='form-control' placeholder="Your Email Address" value="{{ email }}">
				</div>
				<input type="hidden" name="integrity" value='{{ integrity }}'>
				<div class='form-group'>
					<button type='submit' class='btn btn-success' id='forgot'>Reset Password</button>
				</div>
			</form>
			{ if code|length > 0}
			{{ code|flash(message, type)|raw }}
			{ endif } 
		</div>
	</div>
	</div>
	<div class="col-md-6 col-lg-6"></div>
</div>
{ endblock }